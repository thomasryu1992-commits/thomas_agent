# Production runtime dependencies for the Thomas Agent MVP operator service.
#
# The runtime needs only YAML parsing + JSON Schema validation; everything else it uses
# is the Python standard library (json, urllib, hashlib, dataclasses, argparse, pathlib).
# No pytest, no dev/validation tooling.
#
# Pins mirror requirements-validation.lock (the same libraries the CI validators use) so the
# deployed runtime and the validators agree on behavior. Regenerate this in lockstep with that
# file. jsonschema-specifications is a transitive of jsonschema, pinned here for reproducibility.
PyYAML==6.0.3
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
referencing==0.37.0
rpds-py==2026.5.1
attrs==26.1.0
