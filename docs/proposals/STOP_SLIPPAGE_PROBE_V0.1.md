# 제안: 스톱 슬리피지 프로브 — 전략 무장 없이 측정만 산다 (DRAFT v0.1)

**상태:** DRAFT — 설계 선행(구현 아님). 코드 변경 없음. 실주문 능력이 걸린 제안이므로
활성화는 별도 증분 + Thomas 승인.
**작성 배경:** 2026-08-11, Thomas의 "슬리피지 판독이 라이브 재개 뒤라면 재개하자"는 요청에
대한 대안 — 미확증 전략을 무장하는 대신, 측정을 전략 엣지에서 분리한다.
**근거:** `cost.DEFAULT_STOP_SLIPPAGE_BPS`(잠정 12bps, #695 — 08-14 F9 판독 후 머지),
`live_pnl.stop_slippage_observations`(#683), REMAINING_WORK §C의 수기 측정 2건,
`forward_confirmation`(확증 시계 4h ~34주 / 1d ~69주).

---

## 0. 한 줄

스톱 슬리피지 시리즈는 **라이브 스톱 청산에서만 자라는데**, 라이브 재개는 확증 게이트
(#694) 뒤에 있고 확증 시계는 34–69주다. 프로브는 이 순환을 **전략 없이** 끊는다: 최소
규모 포지션 + 지정 스톱 N회로, 비용 모델이 필요로 하는 표본만 사전 상한된 비용에 산다.

## 1. 왜 무장 재개가 답이 아닌가 (기각된 대안)

미확증 계보의 LIVE 무장은 `--allow-unconfirmed-holdout` 웨이버 — #648이 풀 전체를
무장해제했던 조건의 명시 면제 — 를 요구하고, 판정된 계보 전원이 CONTRADICTED인 모집단에서
음(−)의 기대값 거래로 비용 데이터를 사는 구조가 된다. 게이트 출고 당일의 웨이버 선례 비용이
데이터 가치를 초과한다.

## 2. 설계 (초안)

- **1회 프로브** = 시장가 진입(벤처 최소 노셔널) → 즉시 지정 스톱 설치(현행 팩토리 대표
  손절폭, 예: 진입가 대비 30–50bps) → 스톱 체결 시 종료. 스톱이 T bars 내 미체결이면
  시장가 청산 — 그 행은 슬리피지 표본이 아니다(표본은 스톱 체결만).
- **계측은 전부 기존 것 재사용**: #683이 이미 모든 스톱 청산에 `stop_price`를 스탬프하고
  `stop_slippage_bps`를 기록한다. 프로브는 새 측정 코드를 만들지 않고 그 경로로 행을
  만든다.
- **배치 구성** = N회(제안 12): 심볼 3종 × 변동성 레짐 상/하(atr_percentile 분기) ×
  2회, 시간대 분산. 슬리피지는 레짐 종속이므로 한 레짐에 몰린 표본은 상수를 다시
  왜곡한다.
- **오염 없음, 구조적으로**: 프로브 결과 행은 전략 계보를 갖지 않는다(candidate_id /
  generation 없음) — `forward_confirmation.lineage_keys`가 자연 배제하므로 forward 확증
  증거에 한 행도 섞이지 않고, lifecycle 사다리에도 귀속되지 않는다.

## 3. 비용 상한 (사전 계산 가능)

프로브당 최대 손실 ≈ 손절폭 + worst 슬리피지 + 왕복 수수료. 손절폭 40bps + 실측 worst
23.5bps + 수수료 ~10bps ≈ **노셔널의 ~0.75%**. 벤처 최소 노셔널(구현 시 심볼별 확인)이
~100 USDT면 프로브당 ≤0.75 USDT, **12회 배치 총 상한 ≤~9 USDT** — 등록 예산
(주문 ≤200, 일손실 ≤50) 내부이고, 배치 전체가 일손실 한도의 1/5 미만.

## 4. 구현 증분이 통과해야 할 문 (이 제안이 승인되어도 자동으로 열리지 않는 것)

1. **실주문 경로**: 기존 LP4/LP5 어댑터 재사용, 라우팅과 분리된 프로브 전용 진입 사유.
2. **env-gated capability 열거**: `test_the_env_only_gate_has_exactly_the_capabilities_thomas_named`가
   호출 지점을 전수 고정하므로, 프로브 발주 지점은 그 열거에 **명시 추가**되어야 한다 —
   새 능력은 무허가로 출고될 수 없다.
3. **R9 승인**: 배치 1회당 ask 1건(N·심볼·손절폭·예산 상한이 승인 해시에 포함).
4. **브레이커 하위**: 일손실/연속손실 브레이커가 프로브에도 그대로 적용.
5. **가시성**: live_readiness 또는 상태 보드에 프로브 배치의 행(개방 질문 §5-4).

## 5. 열린 결정 (Thomas)

1. N(제안 12)과 심볼 3종(제안 BTCUSDT/ETHUSDT/SOLUSDT — 유동성 상·중위).
2. 손절폭(제안 40bps 고정 — 팩토리 대표 폭; 또는 ATR 연동).
3. 레짐 분기 기준(atr_percentile 상/하위 50%)과 미체결 타임아웃 T.
4. 배치 예산 상한과 보드 표면 형식.
5. **판정 규칙**: 표본 N≥10이면 `DEFAULT_STOP_SLIPPAGE_BPS`의 잠정 12bps를 실측
   중앙값(또는 보수적으로 상위 사분위)으로 대체하는 재결정을 연다 — tunables의
   reopens_when이 이미 이 경로를 가리킨다.
