# 제안: 주식 퍼프 레인 — Hyperliquid HIP-3 위의 팩터/디스퍼전 트레이딩 (DRAFT v0.1)

**상태:** DRAFT — 설계 선행(구현 아님). 코드 변경 없음.
**Thomas 결정 확정 (2026-08-03):** ①"섹터 로테이션"을 **팩터/디스퍼전으로 재정의**하고 진행한다.
②**스프레드(2-leg)는 v1에서 제외** — 보류이며 기각이 아니다(§4). 남은 결정은 §9 하나(규제 판단 기록).
**근거:** 아키텍처 §12–16(확장 기준·가드레일), `CLAUDE.md`(Reuse first, fail-closed),
`docs/REMAINING_WORK.md` §A(예측시장 레인 폐쇄 전례)·§G1(튜너블 소유자 부재),
`docs/runtime-contracts/LP4_ORDER_ADAPTER_DESIGN_V0.1.md`(어댑터 선례).
**선행 관계:** 크립토 레인은 라이브 상태이며 **미해결 결함 보유** — 자율 실주문 2건에서
보호 스탑이 거부되어 포지션을 유지하지 못한다(`REMAINING_WORK.md` §C 머리말). §8의 S5는
그 결함 규명 이후로 정렬된다.

> **이 문서 초안(§4)은 "횡단면 선택 레이어를 새로 만들어야 한다"고 적었고, 그것은 틀렸다.**
> 코드를 읽은 결과 팩터/디스퍼전 기계는 `features.py`와 `factory.py`에 **이미 구현되어 크립토에서
> 돌고 있다**(§3). 그 정정이 이 개정판의 핵심이며, v1의 규모를 크게 줄인다.

---

## 0. 한 줄

**새 도메인 레인도, 새 모듈도 아니다.** 크립토 스택은 (a) 베뉴 어댑터가 이미 `Protocol` 3개로
분리돼 있고, (b) **횡단면 랭킹·상대강도·디스퍼전 피처와 그것을 쓰는 전략 주조 템플릿을 이미
갖고 있다.** 그러므로 주식 퍼프 v1의 실체는 **"베뉴 1개 추가 + 단일 베뉴로 고정된 상수 4개를
베뉴별로 전환 + 비용 모델 재도출"**이다. 새로 발명할 개념은 없다.

## 1. 재사용 지점 ① — 베뉴 이음매는 이미 Protocol 3개다

세 곳 전부 `Protocol` + `select_*` 형태이고, 선택은 세이프티 게이트의 provider 그랜트를 통과한다
(`safety_gate.select_gated`, 파일 1개 = provider 1개).

| 이음매 | 위치 | 현재 구현 | 추가할 것 |
|---|---|---|---|
| 캔들 | [market_data.py:634](../../runtime/mvp_runtime/crypto/market_data.py) `select_market_data_collector` | `Mock` / `BinanceFuturesCollector` | `HyperliquidCollector` |
| 계좌 | [account.py:520](../../runtime/mvp_runtime/crypto/account.py) `select_account_feed` | `NoAccountFeed` / `BinanceFuturesAccountFeed` | `HyperliquidAccountFeed` |
| 주문 | [live_execution.py:574](../../runtime/mvp_runtime/crypto/live_execution.py) `select_order_adapter` | `DryRun` / `BinanceFuturesOrderAdapter` | `HyperliquidOrderAdapter` |

**`venue`는 이미 일급 필드다.** [paper.py:78](../../runtime/mvp_runtime/crypto/paper.py) `DEFAULT_VENUE`,
[live_budget.py:91](../../runtime/mvp_runtime/crypto/live_budget.py) `SUPPORTED_VENUE` 검사, 예산 스키마의
`venue` enum. 다중 베뉴는 **미구현이지만 미설계는 아니다**.

**Reuse first의 귀결:** `runtime/mvp_runtime/stocks/`를 새로 파는 안은 기각한다. 24k LOC이 두 벌이
되고 한 개념(퍼프 포지션·R 회계·라이프사이클)에 authority가 둘이 된다. 패키지명 `crypto/`는
당분간 그대로 둔다 — 개명은 두 번째 베뉴가 실제로 돈 뒤의 별도 결정이다.

## 2. 유니버스 — 좁지만, 이 목적에는 좁은 게 문제가 아니다

HIP-3(2025-10-13 메인넷) 위 실제 상장 (조사 시점 2026-08-03, TradeXYZ가 지배적 배포자):

- 인덱스: `XYZ100`(나스닥100), `SP500`(정식 라이선스 퍼프)
- 단일종목: NVDA, TSLA, AAPL, MSFT (+ GOOGL, AMZN 보고됨)
- 원자재: 금, 은

**헬스케어·에너지·금융·산업재 단일종목이 없으므로 GICS 섹터 로테이션은 구성 불가능하다.**
(Ventuals(비상장 퍼프)는 2026-06에 시장을 닫았다 — 유동성은 소수 배포자로 집약되는 방향.)
Thomas 결정에 따라 대상을 **팩터/디스퍼전**으로 재정의한다: 코호트 내 상대 순위와 벤치마크
대비 초과 모멘텀으로 **어느 종목을 살지 고르고, 단일 레그로 방향성 진입**한다.

**그리고 이 유니버스 크기는 우연히도 기존 기계에 정확히 맞는다.** 크립토 코호트는
`CROSS_SECTION_UNIVERSE`가 **6종목**(BTC/ETH/SOL/BNB/XRP/DOGE) + `REFERENCE_SYMBOL = BTCUSDT`다.
주식은 **6종목**(NVDA/TSLA/AAPL/MSFT/GOOGL/AMZN) + 레퍼런스로 인덱스 1개. **같은 모양이다.**

## 3. 재사용 지점 ② — 팩터/디스퍼전은 이미 구현되어 돌고 있다

`features.py`가 매 바마다 계산하는 컬럼 두 그룹이 정확히 이 전략군의 재료다.

**벤치마크 상대 (`_reference_columns`, [features.py:340](../../runtime/mvp_runtime/crypto/features.py))**
— `ref_roc_4`, `rel_strength_roc_4`(초과 모멘텀), `ref_correlation`(바별 **수익률** 기준),
`ref_market_regime`. 모듈 주석이 존재 이유를 직접 적고 있다: *"다른 모든 컬럼은 심볼 하나를 고립시켜
기술하며, 그래서 풀이 가진 모든 전략이 같은 마켓 베타를 진다."*

**코호트 횡단면 (`_cross_section_columns`, [features.py:107–147](../../runtime/mvp_runtime/crypto/features.py))**
— `xs_rank_pct`(코호트 내 순위 분위), `xs_excess_roc_4`, `xs_dispersion_ratio`(자기 최근 정상 대비
디스퍼전 비율). 셋 다 스케일 프리로 주조 가능하고, 주석이 벤치마크와의 차이를 명시한다:
*"모든 알트가 동시에 BTC를 이길 수 있고, 그 사실은 그중 무엇을 사야 하는지 아무것도 말해주지 않는다."*

**전략 주조 템플릿도 있다** ([factory.py:434–492](../../runtime/mvp_runtime/crypto/factory.py)):
`_rel_strength_long_entry` / `_rel_strength_short_entry`, 그리고 `xs_rank_pct` + `xs_dispersion_ratio`를
결합한 횡단면 롱/숏 진입. **주조 → 풀 채점 → 라이프사이클 판정의 사다리를 그대로 탄다.**

**그러므로 v1에 새 모듈은 없다.** 필요한 것은 아래 상수를 베뉴별로 만드는 일이다.

### 단일 베뉴로 고정된 상수 — 이 제안의 실질 작업

런타임은 정확히 네 곳에서 구조적으로 단일 베뉴다. v1의 본체는 이 싱글턴들을 베뉴 키 조회로 올리는 것이다.

| 상수 | 현재 | 주식 베뉴에서 |
|---|---|---|
| `market_data.REFERENCE_SYMBOL` | `"BTCUSDT"` | 인덱스(`XYZ100` 또는 `SP500`) — §9 선택 |
| `market_data.CROSS_SECTION_UNIVERSE` | 6 크립토 | 6 주식 |
| `paper.DEFAULT_VENUE` / `live_budget.SUPPORTED_VENUE` | `binance_futures` 단수 | 집합 |
| `cost.*` (§5) | 바이낸스 실측/공시 | 재도출 |

**주의 — 코호트 축소에 대한 fail-closed 동작:** `MIN_CROSS_SECTION_MEMBERS = 4`. 6종목 중 3개가
데이터 수집에 실패하면 `xs_*` 컬럼은 전부 None이 되고 횡단면 전략은 발화하지 않는다. 이건 올바른
방향이지만(주석: 코호트 2개면 랭크는 동전 던지기), **얇은 베뉴에서는 바이낸스보다 자주 걸린다.**
S1의 첫 측정 항목이 코호트 가용률이어야 하는 이유다.

**주의 — 인덱스를 거래하면:** `_reference_columns`는 심볼이 레퍼런스 자신일 때 모든 컬럼을 None으로
반환한다(벤치마크의 자기 대비 상대강도는 0.0, 상관은 1.0 — 주조된 조건이 걸릴 수 있는 날조 상수라
비운다). 설계된 동작이며, **XYZ100 자체를 거래하면 그 심볼에서는 상대강도 전략이 아예 발화하지 않는다.**

## 4. 스프레드(2-leg)는 v1에서 제외 — 보류이며 기각이 아니다

Thomas 결정 2026-08-03. 넣었다면 `paper`/`live_leg`가 두 다리를 하나의 R로 회계하는 표현을
새로 가져야 했고, 그게 v1 최대 작업 항목이었다.

**제외의 대가를 정확히 적어둔다.** 숏 레그가 없으면 **이것은 마켓 뉴트럴이 아니다.** 팩터는
*선택*에만 표현되고 *노출*에는 표현되지 않는다 — 상대강도로 NVDA를 골라 롱하면, 결과는
그냥 NVDA 롱이다. 시장이 빠지면 상대강도는 구해주지 않는다. 상대강도 기반 종목 선택은 그
자체로 정당한 전략군이지만, 나중에 "디스퍼전"이라는 말을 보고 뉴트럴이라고 읽으면 안 된다.

**그리고 이 제외는 §6의 상관 캡을 v1의 필수 항목으로 만든다** — 옵션이 아니라. 숏 레그가
빠진 롱 편향 북이 메가캡 테크 6종목 위에서 돌면 상관은 최대가 된다.

**재개 조건(파생, 미비준):** v1 페이퍼가 (a) 코호트 가용률과 (b) `xs_*` 계열 lineage의 ROBUST
후보를 실제로 만들어내고, (c) 그때 측정된 마켓 베타가 단일 레그로 감당 못 할 크기일 때.
`limit-entry` 보류 선례대로, 이 조건들의 출처는 Thomas가 아니라 이 문서이며 정정 대상이다.

## 5. 재사용할 수 **없는** 것 — 상수 복사가 이 설계의 주된 실패 모드

`cost.py`의 숫자는 전부 **바이낸스에서 측정하거나 바이낸스가 공시한 값**이다. 자산군이 아니라
베뉴가 바뀌므로 이식이 아니라 재도출 대상이다.

| 상수 | 현재 | 출처 | Hyperliquid |
|---|---|---|---|
| `FUNDING_INTERVALS_PER_DAY` | 3 | 바이낸스 8h | **24** (시간당, 4%/h 캡) |
| `DEFAULT_FUNDING_BPS_PER_INTERVAL` | 1.0 | 바이낸스 base | 재측정 |
| `DEFAULT_TAKER_FEE_BPS` | 5.0 | 이 계좌 실측 | 재측정 + **HIP-3 배포자 분배(표준 대비 0–300%)** |
| `DEFAULT_MAKER_FEE_BPS` | 2.0 | 바이낸스 공시(미실측) | 재측정 |
| `DEFAULT_SLIPPAGE_BPS` | 3.0 | 실측 | 온체인 북에서 재측정 |

`cost.py` 주석은 펀딩이 *"모델에 없었고 지배적인"* 항이라고 스스로 적는다. 정산이 3회/일 →
24회/일이면 다일 보유의 캐리 항은 성격 자체가 달라진다. 그리고
[cost.py:144](../../runtime/mvp_runtime/crypto/cost.py) `MAX_ENTRY_COST_R = 0.25`는 크립토에서 15m을
**수리한 게 아니라 꺼버린** 게이트다(50행 중 48행 거부). 새 베뉴·새 유니버스에서 무엇이 살아남는지가
S2의 판정 대상이다.

**스탑 모델은 이식 금지. 두 이유가 겹친다.**

1. **현행 결함이 정확히 이 메커니즘에 있다.** 라이브에서 보호 스탑이 두 번 거부됐고 원인 미규명.
   Hyperliquid의 TP/SL은 네이티브 트리거이며 **자동으로 시장가 체결**된다 — 거부된 그것과 다른 물건이다.
2. **갭 리스크.** 주식 퍼프는 24/7 거래되지만 **기초 현물은 아니다.** 주말·야간·특히 실적에서 갭한다.
   현 풀 스탑 중앙값 37.6 bps인데 실적 8% 갭은 그 거리를 20배 이상 건너뛴다. 갭 너머에서 스탑은
   보장이 아니라 **요청**이다. 그리고 **실적 캘린더는 이 시스템에 없는 입력이다.**

**주문 타입:** [live_order.py:195](../../runtime/mvp_runtime/crypto/live_order.py)는 `MARKET`을 고정한다.
Hyperliquid도 시장가를 지원하므로 v1은 유지(리밋 진입 보류는 LP4 스코프 노트의 별건이며 근거는
베뉴별로 다시 따져야 한다). 슬리피지 상수만 재측정 대상.

## 6. 크립토 레인에 없던 리스크

1. **배포자 오라클 리스크.** HIP-3에서는 **빌더가 오라클 가격을 설정한다.** 거래소도 지수제공자도
   아니다. 500k HYPE 스테이킹·슬래싱이 책임 장치지만 바이낸스에도 HL 메인 dex에도 없던 신뢰 차원이다.
   Fail-closed 자세: 단일 상대방이 소스인 마크 가격이 스탑을 트리거하기 전에 **독립 레퍼런스와의
   정합성 검사**를 통과해야 한다.
2. **시장 소멸 리스크.** Ventuals는 2026-06에 시장을 닫았다. 배포자는 시장을 **철수할 수 있다.**
   "포지션을 들고 있는 시장이 더 이상 없다"는 상태를 크립토 레인은 모른다.
3. **상관 리스크 — v1 필수(§4).** [paper.py:116](../../runtime/mvp_runtime/crypto/paper.py)
   `MAX_POSITIONS_PER_SYMBOL = 4`와 방향성 스큐 캡은 **심볼 단위**다. NVDA/AAPL/MSFT/GOOGL에 각각
   롱을 잡으면 전부 통과하고 시스템은 분산됐다고 믿지만 실상은 한 개 베팅이다. 재료는 이미 있다 —
   [indicators.py:256](../../runtime/mvp_runtime/crypto/indicators.py) `rolling_correlation`, 그리고
   피처 행에 이미 실려 있는 `ref_correlation`.
4. **담보·크로스마진.** HIP-3 dex는 각자 quote 자산을 고르고, 배포자를 가로지르는 크로스마진은
   위험을 키운다. 계좌 조회가 다계좌가 된다.

## 7. 키 자세 — 기존 분리와 맞물린다

Hyperliquid는 HMAC이 아니라 **EIP-712 서명(agent/API 지갑 개인키)**을 쓴다. 거버넌스에 유리한
성질: **API 지갑은 서명만 하고, 계좌 데이터를 조회할 수 없으며, 출금할 수 없다.**
[live_execution.py:76](../../runtime/mvp_runtime/crypto/live_execution.py)의 기존 자세("선물 활성,
출금 비활성") 및 [account.py:55](../../runtime/mvp_runtime/crypto/account.py)이 `binance_futures` /
`binance_futures_account`를 **별도 그랜트로 쪼갠 선례**와 그대로 겹친다. 같은 분리를 적용:
`hyperliquid_order`(agent 지갑, 서명 전용) + `hyperliquid_account`(읽기).

**단, 개인키는 API 시크릿과 다른 클래스의 비밀이다** — 온체인 아이덴티티의 bearer 자격증명이고,
API 지갑에는 논스 순서 제약이 있어 리플레이/순서 버그는 HMAC과 다른 실패 모드다.
CLAUDE.md대로 **Claude는 이 키를 만지지 않는다.**

## 8. 시퀀싱 — 데이터 → 페이퍼 → 라이브

- **S0 — 결정 기록.** "퍼프 DEX이므로 예측시장의 규제 문제와 다르다"는 판단을 **근거와 함께 문서화**.
  §A의 교훈: 예측시장 레인은 구두 전제 위에 12,224 LOC과 669 MB를 쌓은 뒤 삭제됐다. 규제 판단은
  Thomas의 것이고 코드보다 **먼저** 기록된다. (§9 — 유일하게 남은 결정)
- **S1 — 베뉴 이음매 + 읽기 전용 수집기.** 기존 Protocol을 구현하는 `HyperliquidCollector`. Mock 우선,
  이후 게이트 통과 네트워크. **주문 없음.** 첫 산출물은 §2 상장 목록의 실측 대체본과
  **코호트 가용률**(§3의 `MIN_CROSS_SECTION_MEMBERS` 위험).
- **S2 — 베뉴별 비용 모델 ⚠️ 하드 게이트.** 펀딩 24회/일, 수수료(배포자 분배 포함), 슬리피지 실측 재도출.
  **이 유니버스에서 cost/R이 `MAX_ENTRY_COST_R`을 크립토 15m처럼 못 넘기면 여기서 멈춘다.**
  싸게 얻는 "아니오"이고, 이 시퀀싱의 존재 이유다.
- **S3 — 베뉴별 상수 전환.** `REFERENCE_SYMBOL` / `CROSS_SECTION_UNIVERSE` / `DEFAULT_VENUE` /
  `SUPPORTED_VENUE`를 베뉴 키 조회로. 새 모듈 없음.
- **S4 — 페이퍼 레그.** paper/pool/lifecycle/factory 무변경 재사용. 팩터/디스퍼전 lineage가 실제로
  ROBUST 후보를 내는지가 판정 대상. 갭 리스크 스탑 모델은 **실측으로 도출**하며 이식하지 않는다.
- **S5 — 라이브.** 예산 스키마 v0.2, 그랜트 2개, **상관 캡(§6.3, v1 필수)**, 시장 소멸 처리.
  **현행 스탑 거부 결함 규명 이후에만 착수** — 그 결함은 베뉴 간 차이가 가장 큰 바로 그 메커니즘에 있다.

부수 등록: `scheduler.KINDS`에 새 kind, `domain_console`에 새 verb — 둘 다 등록 지점이 명시돼 있다.
닫힌 스키마 영향은 `live_trading_budget` **하나뿐**이고, 그 `venue` enum은 스스로
*"코드가 닿을 수 없는 베뉴로 예산이 등록되는 걸 오타가 막지 못하게"* 라고 적은 **설계된 확장 지점**이다.

## 9. 열린 결정 — Thomas (1건)

**규제 판단의 기록(S0).** "퍼프 DEX라 문제없다"의 근거를 어디까지 명시할 것인가. 최소한
`REMAINING_WORK.md`에 한 단락 — 판단 주체, 날짜, 근거, 그리고 **무엇이 바뀌면 재검토하는지**.

*(결정 완료: 팩터/디스퍼전 재정의 ✅ / 스프레드 v1 제외 ✅ — 2026-08-03)*

**선택 사항 — 레퍼런스 심볼:** `XYZ100`(나스닥100, 코호트 6종목과 구성이 겹쳐 베타 상쇄가 큼) vs
`SP500`(광의, 정식 라이선스). S1의 데이터로 판단 가능하므로 지금 결정할 필요는 없다.

## 10. S0 초안 — 아래 §부록 A. 비준되기 전까지 이 문서에 머문다

Thomas가 승인·수정한 뒤 `docs/REMAINING_WORK.md`에 **새 섹션 H**로 옮긴다(§A와 같은 층위).
코드가 실제로 착지하는 시점에 `BUILD_HISTORY.md` 항목이 별도로 생긴다 — 예측시장 판단이
두 곳에 기록된 것과 같은 형태이며, S0 시점에는 기록할 변경이 아직 없다.

---

## 부록 A — S0 결정 기록 초안 (**미비준**, Thomas 확인 필요)

> 아래는 Claude가 잡은 **초안**이다. 규제 판단은 Claude의 것이 아니고 이 문서도 법률 자문이 아니다.
> `〔확인 필요〕` 표시된 곳은 Thomas가 채우거나 지워야 하는 자리이며, 채워지지 않은 채로
> `REMAINING_WORK.md`에 옮겨서는 안 된다.

### H. 주식 퍼프 레인 — **개시 판단 (2026-08-03)**

**판단.** Hyperliquid HIP-3 주식 퍼프는 이 프로젝트가 운영할 수 있는 레인이다 (Thomas, 2026-08-03).
**근거(진술 그대로):** 퍼프 DEX를 통해 진행하므로 §A가 닫은 예측시장의 규제 사유와 다르다.

**진술의 강도를 있는 그대로 적는다.** 원 발언은 *"문제는 안 될 것 같다"* — **잠정 판단이며 법률
확인을 거친 결론이 아니다.** 확정으로 승격했다면 여기를 갱신한다. 〔확인 필요〕

**이 기록이 §A보다 길어야 하는 이유.** §A는 역량을 *제거*했으므로 짧아도 안전했다 — fail-closed.
이 판단은 역량을 *추가*한다. 닫는 판단이 틀리면 대가는 기회비용이고, **여는 판단이 틀리면 대가는
실제 노출이다.** 같은 분량으로 적을 수 없다.

**이 기록을 쓰다가 드러난 것 — 크립토 레인에는 규제 근거 기록이 없다.** 저장소 전체에서 규제
판단 기록은 §A(예측시장 폐쇄) **하나뿐**이다. 그 사이 2026-08-02에 런타임은 바이낸스 선물에서
**자율 실주문 2건을 실행했다.** 즉 살아 있는 머니 패스가 예측시장 레인을 죽인 것과 **같은 종류의
미기록 전제** 위에서 돌고 있다. 〔확인 필요: 이 기록에 크립토 레인 근거를 함께 적을지, 별건으로 둘지〕

**그래서 질문의 정확한 모양.** 이 프로젝트는 **이미** 해외 거래소에서 자기 계산으로 파생상품
실주문을 실행했다(바이낸스 선물, 라이브). 그러므로 여는 질문은 "역외 퍼프를 거래해도 되는가"가
아니다 — 그건 실무로 이미 답해져 있다. 남는 질문은 **"기초자산이 미국 주식이라는 점이 그 판단을
바꾸는가"** 하나다. 〔확인 필요 — 이 축소가 맞는지가 이 단락의 핵심〕

**재검토 조건 — 무엇이 바뀌면 이 판단을 다시 연다.**

1. **접근 조건이 바뀔 때.** 배포자(TradeXYZ 등)가 한국 거주자 접근을 제한하거나, 반대로 KYC를
   도입해 신원이 베뉴에 귀속되는 경우. 지금 "DEX라서" 성립하는 전제가 바로 이 지점이다.
2. **인가의 대상이 바뀔 때.** 주식 퍼프가 *기초자산 라이선스*가 아니라 **베뉴 자체의 인가**를
   요구하는 방향으로 규제·행정해석이 움직이는 경우. (`SP500` 퍼프가 "정식 라이선스"인 것은
   지수 사용에 대한 것이며, 베뉴가 한국 이용자에 대해 인가받았다는 뜻이 아니다.)
3. **자기 계산을 벗어날 때.** 타인 자금·권유·자문이 개입되는 순간. **이 조건은 레인이 아니라
   사용 방식에 달려 있고 지금은 성립하지 않는다** — 그러나 성립하면 판단 전체가 다른 법역으로 옮겨간다.
4. **의무가 실행과 분리되지 않을 때.** 해외 파생상품 신고·과세 의무는 이 레인을 막지 않지만,
   원장이 그 의무를 지탱해야 한다. 지금 원장이 무엇을 남기는지는 S1에서 확인한다.

**Claude의 위치.** 이 판단은 Thomas의 것이다. Claude는 이 레인의 키를 다루지 않고, 라이브를 켜지
않으며, 이 단락은 받아적은 것이지 법률 판단이 아니다.

**이 기록이 없으면 무슨 일이 일어나는가 — §A가 남긴 교훈.** 예측시장 레인은 구두 전제 위에
12,224 LOC과 669 MB를 쌓았고, 규제 판단이 뒤늦게 내려지자 전부 삭제됐다. PM1은 엣지가 실재하는지
**판정에 도달하지도 못했다** — 창이 부정적 결과가 아니라 규제 발견으로 잘렸기 때문이다.
S0가 S1보다 앞에 있는 이유가 이것이고, 이 문단은 초안에서 지우지 말 것.

---

## 부록 B — 이 설계가 스스로 점검한 것

- **§16 가드레일("미래 가능성을 위해 미리 만들지 말 것"):** S2를 하드 게이트로 둔 이유. 비용
  모델이 통과 못 하면 S3 이후는 짓지 않는다.
- **§G1(602개 튜너블에 소유자 없음):** 이 제안의 실질 비용은 코드가 아니라 여기다. 두 번째 베뉴는
  소유자 없는 상수 표면을 **소유자가 생기기 전에 늘린다.** S2가 게이트여야 하는 두 번째 이유.
  다만 §3의 정정 이후 늘어나는 표면은 초안 추정보다 훨씬 작다 — 새 모듈이 아니라 상수 4묶음이다.
- **아키텍처 §13(에이전트 분리 기준):** 이 제안은 판단 주체를 분리하지 않는다. 분리하는 것은
  **베뉴와 유니버스**이므로 §13은 발동하지 않는다.
- **미검증으로 남긴 것:** HIP-3 상장 목록·수수료 구조는 2026-08-03 공개 자료 기준이며 베뉴에 직접
  질의하지 않았다. S1의 첫 산출물이 실측 대체본이어야 한다.
